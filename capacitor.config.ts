import type { CapacitorConfig } from '@capacitor/cli';
const config: CapacitorConfig = { appId: 'com.mobowar.game', appName: 'Mobo War', webDir: 'dist', bundledWebRuntime: false };
export default config;
